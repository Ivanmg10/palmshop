export type Game = {
  name: string;
  image: string;
  console: string;
};

export type Item = {
  category: string;
  description: string;
  id: number;
  image: string;
  price: number;
  title: string;
};
